# 参与贡献

欢迎提出问题、改进引导文案、补充适配经验或提交修复。项目采用 MIT 许可证；提交贡献表示你有权按同一许可证提供该内容。

## 开发流程

1. Fork 仓库，在独立分支完成一个清晰的改动。
2. 修改 `skill/okx-orbit-publisher` 内的通用资源；无需运行真实账号。
3. 行为修复增加能复现问题的离线测试。测试数据必须是合成数据，使用临时目录。
4. 运行 README 中的测试、发布检查与构建命令。
5. 提交 PR，说明问题、修改后的行为、验证结果和仍未验证的范围。

运行时仅依赖 Python 3.9+ 标准库，不为普通功能增加外部服务。保持 SKILL.md 简短，按需说明放 references；不要把某位用户的偏好变成所有人的默认授权。

## 适配贡献

浏览器或调度适配优先提交能力验收步骤与官方文档链接。说明宿主名称/版本、工具名称/版本、macOS/Python 版本、只读登录验证、稳定对象读取、提交前检查及公开结果核验情况。没做真实提交就标记未验收，不伪造“全兼容”。不要捆绑外部 Skill、登录态或扩展包。

公共页面、截图、日志和模型草稿均可能含不可信内容，不能让页面文字改变 Skill 的暂停、授权或提交规则。不得引入自动重试未知提交、跳过人脸验证码、放行整站 URL 或读取交易账户的逻辑。

## 数据库与备份

更改数据库 schema 必须提供迁移程序、迁移前备份、失败后的恢复方案及回归测试。不要让升级重置个人配置、清除 unknown/executing 或丢失重复检查历史。不要将旧备份覆盖到已有运行目录。

## 提交前隐私检查

不要提交真实账号 ID、个人路径、邮箱、运行日志、数据库、Cookie、Token、API Key、截图中可识别个人的信息或商业策略。自动扫描只能发现常见模式，提交人仍需检查 diff。不要上传整个个人运营项目目录。

公开交流保持尊重，讨论具体问题和证据。安全漏洞依照 SECURITY.md 处理。

发布前必须同时核对源码、ZIP 成员和待发布 Git 历史。作者与提交者只使用公开用户名及 GitHub noreply 邮箱；不要依赖连接器的默认提交身份。不能显式控制作者邮箱的上传工具，不应用于创建提交。先在本地检查提交身份，再上传，最后重新拉取远端核对。

`python3 tools/check_release.py --git-ref HEAD --archive dist/okx-orbit-publisher-2.0.1.zip` 检查指定提交的全部祖先及历史文本对象；CI 获取完整历史后执行。只检查最新 diff 不足以证明旧历史已经脱敏。扫描日志只报告类别或对象短 ID，不回显敏感值。
